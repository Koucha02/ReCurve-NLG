本项目基于纳岭沟地区测井数据，完成曲线重构以及岩性识别两项任务。

起初打算把.las转换成xls格式，但是后续发现了直接读取.las的方法，所以ExtractAndRenameData里面关于txt2xlsx的内容可以忽略；

lasOP用来画图和其他las操作，先从图中找到数据的问题，以便后续预处理；
现在图的问题是
1.刻度过于密集，需要取消刻度；
2.曲线过于密集，考虑是长度过长，所以只截取其中一部分作图；

根据曲线情况，我们只选取其中200m~300m的测井曲线用作画图；然后选取100~300米的曲线用作训练数据；

明天的任务是把训练样本做好裁剪，然后把模型里面的数据换成我们的数据；

